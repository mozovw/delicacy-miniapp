server:
  port: 8090
  servlet:
    context-path: /auth

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 123456
    url: jdbc:mysql://localhost:3306/oauth?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: root

oauth:
  clientDetailsService: memery
security:
  oauth2:
    resource:
      id: auth-server